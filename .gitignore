node_modules/
dist/
.env
server/dj-pro
server/server
*.log
# runtime state (queue/room snapshot), at any depth
**/data/
# built SPA copied in by `make build`; only the embed placeholder is tracked
server/web-dist/*
!server/web-dist/.gitkeep
